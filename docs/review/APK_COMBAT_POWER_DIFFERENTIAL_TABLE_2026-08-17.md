# APK 战力差分测试表（2026-08-17）

范围：只验证 `data/apk-canonical/catalogs/combat-power-runtime-evidence.json` 已登记的 APK `kl` 字段映射；总值仍是派生值，不写回 Player。

| 维度 | 来源口径 | 自动验证锚点 | 状态 |
|---|---|---:|---|
| 人类基础 | `Vs(level)` | 24 级基础 `42` | confirmed |
| 魂兽基础 | `js(beastYears)` | 1000 年基础 `100` | confirmed |
| 魂兽称号 | `We(base, sum(pl))`，不是直接相加倍率 | 1000 年 + `王`：称号 `10`、总值 `110` | fixed |
| 魂兽血脉 | 单血脉 `We(base, Ui)`；混合血脉按占比、`minimum=false` | 纯龙 `50`；纯龙/低等 50/50 融合 `0` | fixed |
| 状态 | 总构成后应用 5000/15000 bp 状态倍率 | 24 级 +10、重伤：总值 `26` | confirmed |
| 魂环 | 年限基础 + 品质/神赐差分 | 50 年神赐环：环基础 `1`、品质 `1` | confirmed |
| 魂骨 | 普通骨基础/品质；神装部件只在资格门槛后转为 `×3` | 99 级 `godArmor=0`；100 级三级神装 `324` | fixed |
| 神装/神位 | 人类 `level >= 100` 才进入神装套与神位；魂兽按百万年渡劫资格 | 人类 100 级 `godhood=300` | fixed |
| 神器 | 完整神器固定战力；人类 100 级以下除以 10，魂兽不除 | 人类 99 级 `30`；魂兽 `300` | fixed |

未登记的状态不返回近似总值：状态倍率、血脉条目、完整神器来源和显式未覆盖标记统一抛出 `APK_COMBAT_POWER_UNCOVERED_STATE`。本表不宣称 APK 全部路线或全部未来状态已闭合。
